##### xubuntu ######
https://github.com/sgerrand/xfce4-terminal-colors-solarized

####################

*************
ubuntu server

xserver-xorg
lightdm-gtk-greeter

*********

## Use lynx and wget to download all files on a webpage
lynx --dump http://www.rabbitmq.com/releases/rabbitmq-server/v2.8.4/ | grep www.rabbitmq.com/releases | awk '{print $2}' > files
for i in $( cat files ); do wget $i; done

## mount a samba (windows) drive on linux
sudo mount //host/path /mnt/dest -o username=wpeterson,password='my_password'


# make a basic tunnel on localhost port 80 to port 80 on host2 tunneling through host1
ssh -L 80:host2:80 host1 


#### VIM
#pretty tsv columns
:%!column -t


#### Sys admin stuff:
Get info about hard drives and raids:
cat /proc/mdstat
mdadm --query /dev/md0
hdparm -I /dev/sdb
search hex:
/\%x1b

Show character under cursor
set statusline=%<%f%h%m%r%=%b\ 0x%B\ \ %l,%c%V\ %P

turn nbsp (0xA0) into space (0x20) after copy / paste from web:
%s/\%xA0/ /g
